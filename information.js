const monObjet =  {
    firstName : `Fanny`,
    lastName : `B`,
    campus : `Marseille`,
}

module.exports = {
 fullName: `${monObjet.firstName} ${monObjet.lastName}`,
 campus: `${monObjet.campus}`,   
}